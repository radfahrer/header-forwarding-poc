{
  "name": "header-forward-test",
  "version": "1.0.0",
  "description": "Test to show that an axios backed express app can forward headers without adding mental overload for devs. ",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
	"start": "node --inspect server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.17.1",
    "express": "^4.16.2"
  }
}
